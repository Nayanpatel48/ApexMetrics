name: exercise_tracker
description: Exercise data tracker with AI insights

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  appwrite: ^17.1.0
  file_picker: ^6.0.0
  fl_chart: ^0.65.0
  http: ^1.1.0
  csv: ^5.0.2

  # webflow dependency
  webview_flutter: ^4.8.0
  google_fonts: ^6.2.1 # For custom fonts

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
